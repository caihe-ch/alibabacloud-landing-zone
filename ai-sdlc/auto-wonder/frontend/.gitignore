node_modules
dist
*.local
# TypeScript incremental build cache; regenerated on every build and a
# constant source of merge conflicts between parallel branches.
*.tsbuildinfo

# Vitest coverage output; regenerated by `npm run test:coverage`.
coverage
